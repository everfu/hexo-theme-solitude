span.tags-punctuation
  display flex
  align-items center

.bber-reply
  cursor pointer
  margin 0 4px

  &:hover
    color var(--efu-main)

.post-tips
  color var(--efu-gray)
  font-size 14px;
  position absolute
  bottom 1rem
  left 2rem

  +maxWidth768()
    left 1rem

  a
    color var(--efu-gray)
    border none
    margin-left 4px

    &:hover
      color var(--efu-main)