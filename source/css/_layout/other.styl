span.tags-punctuation
  margin-right 2px
  font-size .7rem
  font-weight 700
  display flex
  align-items center

.bber-reply
  cursor pointer
  margin 0 4px

  &:hover
    color var(--efu-main)

.post-tips
  color var(--efu-gray)
  font-size 14px;
  position absolute
  bottom 2rem
  left 2rem

  +maxWidth768()
    left 1rem

  a
    color var(--efu-gray)
    border none

    &:hover
      color var(--efu-main)